{
  "name": "lines-of-caffeine",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app",
    "test" : "NODE_ENV=test mocha --recursive --reporter spec --growl",
    "watch" : "NODE_ENV=test mocha --growl --watch --recursive --reporter spec"
  },
  "engines": {
    "node": "0.8.x",
    "npm": "1.1.x"
  },  
  "dependencies": {
    "express": "3.0.5",
    "jade": "*",
    "mongoose": "*",
    "twilio": "*",
    "getenv": "*",
    "underscore" : "*",
    "underscore.string": "*",
    "newrelic": "*",
    "node-mandrill": "*"
  },
  "devDependencies": {
    "mocha" : "*",
    "should" : "*",
    "supertest" : "0.7.x"
  }
}